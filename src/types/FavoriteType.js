/**
 * Favorite type options when getting user favorites.
 * @type {{PLAYLISTS: string, TRACKS: string, ARTISTS: string, ALBUMS: string, VIDEOS: string}}
 */
const FavoriteType = {
    ALBUMS: "albums",
    ARTISTS: "artists",
    PLAYLISTS: "playlists",
    TRACKS: "tracks",
    VIDEOS: "videos"
};

export default FavoriteType;
